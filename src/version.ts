// Single runtime version constant for ASR.
//
// Keep in sync with the `version` field in package.json and the `version`
// field in extension/manifest.json when bumping a release.
export const ASR_VERSION = "0.2.0";
